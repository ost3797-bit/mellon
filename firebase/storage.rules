rules_version = '2';
service firebase.storage {
match /b/{bucket}/o {
match /uploads/{uid}/{allPaths=**} {
allow write: if request.auth != null && request.auth.uid == uid; // 자신의 폴더만 업로드 가능
allow read: if true; // 공개 읽기 (필요 시 Firestore 승인과 연동해 더 엄격히 할 수 있음)
}
}
}